/* 1. Criar uma função que exibe "Olá, mundo!" no console.
    function exibirOlaMundo() {
        console.log('Olá, mundo!');
    }
    exibirOlaMundo();
*/
/* 2. Criar uma função que recebe um nome como parâmetro e exibe "Olá, [nome]!" no console.
    let nome = prompt('Digite seu nome: ');
    function exibirOlaNome(name) {
        console.log(`Olá, ${name}!`);
    }
    exibirOlaNome(nome);
*/
/* 3. Criar uma função que recebe um número como parâmetro e retorna o dobro desse número.
    let numero = parseInt(prompt('Digite um número qualquer: '));
    function calcularDobro(numero) {
        return numero * 2;
    }
    console.log(calcularDobro(numero));
*/
/* 4. Criar uma função que recebe três números como parâmetros e retorna a média deles.
    let numero1 = parseInt(prompt('Digite o primeiro número: '));
    let numero2 = parseInt(prompt('Digite o segundo número: '));
    let numero3 = parseInt(prompt('Digite o terceiro número: '));
    function calcularMedia(num1, num2, num3) {
        let media = (num1 + num2 + num3) / 3;
        return media;
    }
    console.log(calcularMedia(numero1, numero2, numero3));
*/
/* 5. Criar uma função que recebe dois números como parâmetros e retorna o maior deles.
    let numero1 = parseInt(prompt('Digite o primeiro número'));
    let numero2 = parseInt(prompt('Digite o segundo número'));
    function verificarMaior(num1, num2) {
        return num1 > num2 ? num1 : num2;
    }
    console.log(verificarMaior(numero1, numero2));
*/
/* 6. Criar uma função que recebe um número como parâmetro e retorna o resultado da multiplicação desse número por ele mesmo.
    let numero = parseInt(prompt('Digite o número: '));
    function potenciacaoDoNumero(num) {
        let resultado = num * num;
        return resultado;
    }
    console.log(potenciacaoDoNumero(numero));
*/