/*  1. Crie um contador que comece em 1 e vá até 10 usando um loop while. Mostre cada número.
    let contador = 0;
    while (contador <= 10) {
        alert(`O número atual do contador é: ${contador}`);
        contador++;
    }
*/
/*  2. Crie um contador que começa em 10 e vá até 0 usando um loop while. Mostre cada número.
    let contador = 10;
    while (contador >= 0) {
        alert(`O número atual do contador é: ${contador}`);
        contador--;
    }
*/
/*  3.  Crie um programa de contagem regressiva. Peça um número e conte deste número até 0, usando um loop while no console do navegador.
    let contagem = prompt('Digite o número máximo da contagem.');
    while (contagem >= 0) {
        alert(`O número atual na contagem regressiva é: ${contagem}`);
        contagem--;
    }
*/
/*  4.  Crie um programa de contagem progressiva. Peça um número e conte de 0 até esse número, usando um loop while no console do navegador.
    let numeroMaximo = prompt('Digite o número máximo da contagem progressiva: ');
    let contagem = 0;
    while (contagem <= numeroMaximo) {
        alert(`O número atual na contagem progressiva é: ${contagem}`);
        contagem++;
    }
*/